extends layout

block content
  h1 Niðurstöður margföldunnar

  p Þú slóst inn:
  p #{input}
  p Úr því fundust tölurnar:
  p #{foundNumbers}
  p Margfeldið af þeim er:
  p #{multiplicationResults}
  p Þættir eru:
  p #{factors}

  hr

  p Endurtaka leikinn:

  // multiplication form
  form(method='POST' action='/')
    label(for='multiplicationbox') Tala eða tölur
    textarea#multiplicationbox.form-control(type='text', rows="5", name="multiplicationbox", placeholder="")
    button.btn.btn-primary(type='submit') Margfalda og finna þætti